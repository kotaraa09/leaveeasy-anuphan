# ACL.md — ตารางสิทธิ์ของ LeaveEasy

ใครทำอะไรได้ ทำอะไรไม่ได้ แยกตาม 3 บทบาทในช่อง `role` ของโฟลเดอร์ `users`
ค่าที่เก็บจริงในฐานคือรหัสอังกฤษ `employee` · `manager` · `hr` ส่วนคำไทยใช้แสดงบนหน้าจอเท่านั้น

---

## ตารางสิทธิ์

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | ยื่นใบขอลาใหม่ (สถานะเริ่มที่ `รอพิจารณา` เสมอ) · ดูใบลา **ของตัวเอง** · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่ยังเป็น `รอพิจารณา` · ดูรายชื่อประเภทการลา | **อนุมัติหรือไม่อนุมัติใบลาของตัวเอง** · เปลี่ยนสถานะใบลาของใครก็ตาม · **ดูใบลาของคนอื่น** · เขียนความเห็นในใบของคนอื่น · ลบใบของตัวเองที่ตัดสินไปแล้ว (`อนุมัติ` / `ไม่อนุมัติ`) · เพิ่ม แก้ ลบ ประเภทการลา · กำหนดผู้อนุมัติให้ใบลา · แก้ `role` ของตัวเอง |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลา **ทุกใบ** · เปลี่ยนสถานะใบลาของคนอื่นเป็น `อนุมัติ` / `ไม่อนุมัติ` · เขียนความเห็นได้ในทุกใบ · ยื่นใบขอลาของตัวเอง (ในฐานะผู้ขอลา) | **อนุมัติหรือไม่อนุมัติใบลาของตัวเอง** · ลบใบลาของคนอื่น · เปลี่ยนสถานะใบที่ตัดสินไปแล้ว · กด `ไม่อนุมัติ` โดยยังไม่มีความเห็นในใบนั้นอย่างน้อย 1 รายการ · เพิ่ม แก้ ลบ ประเภทการลา · กำหนดผู้อนุมัติให้ใบลา · แก้ `role` ของใคร |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · **กำหนดผู้อนุมัติให้แต่ละใบ** · **เพิ่ม แก้ ลบ ประเภทการลา** · เห็นแดชบอร์ดสรุปทั้งระบบ · ยื่นใบขอลาของตัวเอง (ในฐานะผู้ขอลา) | **อนุมัติหรือไม่อนุมัติใบลาของตัวเอง** · ลบใบลาของคนอื่น · เปลี่ยนสถานะใบที่ตัดสินไปแล้ว · เพิ่มสถานะที่ 4 นอกจาก 3 ค่าที่กำหนดไว้ · แก้ `role` ของผู้ใช้จากหน้าเว็บ (ต้องไปแก้ใน Firebase Console) |

---

## กติกาที่ใช้ร่วมกันทุกบทบาท

- **ห้ามอนุมัติใบของตัวเอง** — ถ้า `requesterId` เท่ากับ `uid` ของคนที่ล็อกอินอยู่ ปุ่ม **อนุมัติ / ไม่อนุมัติ** ต้องไม่ขึ้น
  ต่อให้เป็น `manager` หรือ `hr` ก็ตาม เพราะคนคนเดียวเป็นทั้งผู้ขอและผู้ตัดสินไม่ได้
- **ปุ่มตัดสินขึ้นเฉพาะใบที่ยังเป็น `รอพิจารณา`** — ใบที่ตัดสินไปแล้วถือว่าปิดจบ
- **จะกด `ไม่อนุมัติ` ได้ ต้องมีความเห็นในใบนั้นอย่างน้อย 1 รายการก่อน**
- **สถานะมี 3 ค่าเท่านั้น** — `รอพิจารณา` · `อนุมัติ` · `ไม่อนุมัติ` ห้ามเพิ่มค่าที่ 4 และห้ามใช้คำอังกฤษ
- **คนสมัครใหม่ได้ `role` เป็น `employee` เสมอ** — หน้าสมัครไม่มีตัวเลือกบทบาท
  จะเลื่อนเป็น `manager` / `hr` ต้องเข้าไปแก้ใน Firebase Console

---

## สถานะการบังคับใช้ตอนนี้

| ชั้น | บังคับแล้วหรือยัง |
|---|---|
| 🖥️ **หน้าจอ** (`js/acl.js`) | ✅ ซ่อนเมนูและปุ่มที่บทบาทนั้นใช้ไม่ได้ · หน้ารายการถามฐานเฉพาะใบของตัวเองเมื่อผู้ใช้เป็นผู้ขอลา |
| 🛡️ **ฐานข้อมูล** (`firestore.rules`) | ✅ เขียนครบทุกข้อในตารางข้างบนแล้ว — **ยกเว้น 1 ข้อ** (ดูข้างล่าง) · ⚠️ **ต้องกด Publish ที่ Firebase Console ก่อน กฎถึงมีผลจริง** |

> 🚫 **ข้อเดียวที่เขียนลง `firestore.rules` ไม่ได้** — "จะกดไม่อนุมัติได้ ต้องมีความเห็นอย่างน้อย 1 รายการก่อน"
> เพราะกฎนับจำนวนไฟล์ในโฟลเดอร์ย่อยไม่ได้ (เปิดได้ทีละไฟล์ที่รู้ชื่อเท่านั้น)
> ข้อนี้จึงยังบังคับอยู่ที่ `js/leave-request-detail.js` ฝั่งเดียว — ใครยิงคำสั่งจาก Console ก็ข้ามได้

> ⚠️ **สองชั้นนี้ทำหน้าที่ต่างกัน** — ชั้นหน้าจอกันคนกดผิดและทำให้หน้าจออ่านง่าย
> **ยามเฝ้าตัวจริงคือ `firestore.rules`** ใครเปิด Console ของเบราว์เซอร์ก็ยิงคำสั่งข้าม `js/acl.js` ได้
> แก้กติกาข้อไหนต้องแก้ให้ตรงกันทั้งสองที่เสมอ ไม่งั้นหน้าจอกับฐานจะเชื่อคนละอย่าง

**แก้ตารางข้างบนแล้วต้องตามไปแก้ที่ไหนบ้าง**

| ไฟล์ | ทำอะไร |
|---|---|
| `js/acl.js` | กติกาทุกข้อในตารางนี้ แปลงเป็นฟังก์ชัน `ตัดสินใบลาได้()` `ลบใบลาได้()` `ดูใบลาได้()` … — **แก้ที่นี่ที่เดียว** |
| `js/auth.js` | ซ่อน/เปิดสิ่งที่เขียนไว้ใน HTML ว่า `data-เฉพาะบทบาท="hr"` |
| `js/nav.js` | เมนูไหนจำกัดบทบาทบ้าง |
| `firestore.rules` | ยามเฝ้าตัวจริง — แก้แล้วต้อง Publish ที่ Firebase Console ทุกครั้ง |

> ⚠️ ข้อ **"ดูใบลาของคนอื่นไม่ได้"** ผูกกับ `requesterId` ที่เป็น `uid` จริงจาก Firebase Auth
> ใบตัวอย่าง `lr001`–`lr005` ใช้ `u001`–`u003` ซึ่งไม่ผูกกับบัญชีของใคร
> พอกรองด้วย `uid` จริง ใบพวกนี้จะไม่ขึ้นให้ใครเห็นเลย — ต้องตัดสินใจก่อนว่าจะแก้ให้ชี้ `uid` จริง หรือลบทิ้ง
